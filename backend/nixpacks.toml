# Nixpacks configuration for Deno backend on Railway

[phases.setup]
nixPkgs = ["deno"]

[phases.build]
cmds = ["deno cache main.ts"]

[start]
cmd = "deno run --allow-net --allow-env --allow-read main.ts"
